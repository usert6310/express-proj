pipeline {

    agent any

    stages {

        stage("build") {

            steps {
                echo "inbuild"
            }
        
        stage("test") {

            steps {
                echo "in test"
            }
        }

        stage("deploy") {

            steps {
                echo "in deploy"
            }
        }

        }
    }
}